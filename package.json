{
  "name": "routerlens",
  "private": true,
  "description": "Renders INSYS icom OS config exports as readable documentation, fully client-side.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
